export type Comb = {
  id: number
  name: string
  color: {
    primary: string
    secondary: string
  }
};

export type Honey = {
  id: number
  name: string
  color: {
    primary: string
    secondary: string
  }
};
